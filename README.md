# Hyprland Configuration

This repository contains my personal Hyprland window manager configuration for CachyOS.

## 📁 Repository Structure

```
.config/
├── .gitignore          # Git ignore rules (excludes other .config files)
└── hypr/
    └── hyprland.conf   # Main Hyprland configuration
```

## 🚀 Features

- **Window Management**: Dwindle layout with smart gaps
- **Animations**: Smooth bezier curve animations
- **Keybindings**: Vim-style navigation and workspace management
- **Turkish Keyboard Layout**: Configured for TR layout
- **Media Controls**: Volume, brightness, and media player controls
- **Applications**: 
  - Terminal: Kitty
  - File Manager: Dolphin
  - Application Launcher: Wofi

## 🛠️ Installation

1. Clone this repository to your home directory:
   ```bash
   git clone <your-repo-url> ~/.config-hypr
   ```

2. Copy the configuration files:
   ```bash
   cp -r ~/.config-hypr/hypr ~/.config/
   ```

3. Restart Hyprland or reload the configuration:
   ```bash
   hyprctl reload
   ```

## ⌨️ Key Bindings

| Key Combination | Action |
|---|---|
| `Super + Q` | Open terminal (Kitty) |
| `Super + C` | Close active window |
| `Super + M` | Exit Hyprland |
| `Super + E` | Open file manager (Dolphin) |
| `Super + R` | Open application launcher (Wofi) |
| `Super + V` | Toggle floating mode |
| `Super + Arrow Keys` | Move focus |
| `Super + [1-9,0]` | Switch to workspace |
| `Super + Shift + [1-9,0]` | Move window to workspace |
| `Super + S` | Toggle scratchpad |

## 🎨 Customization

The configuration includes:
- **Gaps**: 5px inner, 20px outer
- **Borders**: 2px with gradient colors
- **Rounding**: 10px corner radius
- **Blur**: Enabled with vibrancy
- **Shadows**: Enabled with 4px range

## 📝 Notes

- This repository only tracks Hyprland-specific configuration files
- Other `.config` files are intentionally excluded via `.gitignore`
- Configuration is optimized for CachyOS
- Remove `autogenerated = 0` line from hyprland.conf if you see warnings

## 🔄 Syncing Changes

To sync changes:
```bash
cd ~/.config
git add hypr/
git commit -m "Update Hyprland configuration"
git push
```

## 📚 Resources

- [Hyprland Wiki](https://wiki.hypr.land/)
- [Hyprland Configuration Guide](https://wiki.hypr.land/Configuring/)
- [CachyOS Documentation](https://wiki.cachyos.org/)